import $$EmptyStack from '../.smartface/generated/routers/components/EmptyStack';

export default class EmptyStack extends $$EmptyStack {
  constructor(basePath: string, name: string, routeIndex: number) {
    super(basePath);
  }
}
