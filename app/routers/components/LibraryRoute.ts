import $$LibraryRoute from '../.smartface/generated/routers/components/LibraryRoute';

export default class LibraryRoute extends $$LibraryRoute {
  constructor(basePath: string, name: string, routeIndex: number) {
    super(basePath);
  }
}
